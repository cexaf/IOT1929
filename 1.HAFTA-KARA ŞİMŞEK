void setup() {
 pinMode(3,OUTPUT);
 pinMode(4,OUTPUT);
 pinMode(5,OUTPUT);
 pinMode(6,OUTPUT);
 pinMode(7,OUTPUT);
 
}

void loop() {
  for(int b=3; b < 8 ; b++){
    digitalWrite(b , HIGH);
    delay(500);
  }
  for(int b=3; b < 8 ; b++){
    digitalWrite(b , LOW);
    delay(500);
  }
  

}
